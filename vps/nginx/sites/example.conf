# Template for adding additional subdomains/applications.
# Copy this file, rename it, and update server_name and proxy_pass.

server {
    listen 80;
    server_name app.yourdomain.com;

    location / {
        proxy_pass http://app-container-name:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Uncomment after SSL:
# server {
#     listen 443 ssl http2;
#     server_name app.yourdomain.com;
#
#     ssl_certificate /etc/nginx/ssl/app/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/app/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://app-container-name:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
